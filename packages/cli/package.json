{
  "name": "@favtunes/cli",
  "private": true,
  "version": "1.0.0",
  "description": "favtunes cli",
  "author": "lobsterbandit <code.lobsterbandit@outlook.com>",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "yarn run clean && tsc -b --force",
    "clean": "rimraf build",
    "prepare": "yarn run clean && tsc -b --force",
    "start": "ts-node -r dotenv/config src/index.ts"
  },
  "dependencies": {
    "@favtunes/pandora": "^1.0.0",
    "dotenv": "^8.1.0"
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "ts-node": "^8.3.0"
  }
}
