{
  "name": "@tune-sync/cli",
  "private": true,
  "version": "1.0.0",
  "description": "tune-sync cli",
  "author": "lobsterbandit <code.lobsterbandit@outlook.com>",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "yarn run clean && tsc -b --force",
    "clean": "rimraf build",
    "prepare": "yarn run clean && tsc -b --force",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "@tune-sync/pandora": "^1.0.0",
    "yargs": "14.0.0"
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "ts-node": "^8.3.0"
  }
}
